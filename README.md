# simple-time-service
Particle 41 Assessment

The first step would be loading dependencies

Install Docker
Docker Account
Public Repo

Create a Repo and add flask file amd DockerFile
Build Docker Image : Use a minimal base image, Create a non-root user, Run on 5000 port
Run Docker Container Locally
Tag and Push to DockerFile
Deploy and Test
Push code to Repo

